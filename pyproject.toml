[project]
name = "alumnisync-new"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0.2",
    "django-filter>=25.2",
    "django-jazzmin>=3.0.3",
    "pillow>=12.1.1",
]
